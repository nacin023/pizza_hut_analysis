CREATE TABLE pizza_hut_locations (
    id SERIAL PRIMARY KEY,
    type VARCHAR(255),
    address_1 VARCHAR(255),
    address_2 VARCHAR(255) NULL,
    open_hours VARCHAR(255),
    city VARCHAR(255),
    state VARCHAR(2),
    postal_code INT,
    latitude FLOAT,
    longitude FLOAT
);
